import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  sepratorLineStyle: {
    borderWidth: 0.8,
    width: '100%',
    borderColor: 'grey',
    marginVertical: 10,
  },
});
export default styles;
