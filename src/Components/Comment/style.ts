import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#a7a7a7',
    borderRadius: 10,
    padding: 5,
  },
  imageAndUserWrapper: {
    flexDirection: 'row',
    alignItems: 'center',
    columnGap: 10,
  },
  body: {marginStart: 50, marginEnd: 20},
});
export default styles;
